{
  "name": "cloudnextra-whatsapp-bot",
  "version": "1.0.0",
  "description": "WhatsApp bot for Render deployment",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "npm install --platform=linux --arch=x64 sharp",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.4.0",
    "dotenv": "^16.3.1",
    "node-fetch": "^2.7.0",
    "pino": "^8.15.0",
    "qrcode-terminal": "^0.12.0",
    "sharp": "^0.32.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
